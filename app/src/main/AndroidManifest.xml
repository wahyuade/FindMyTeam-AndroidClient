<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.wahyuade.findmyteam">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/findmyteam"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".new_app.LoginActivity" />
        <activity android:name=".new_app.RegisterActivity" />
        <activity android:name=".dashboard.user.DashboardUser" />
        <activity android:name=".dashboard.admin.DashboardAdmin" />
        <activity android:name=".menu.OpenMenuAdminActivity" />
        <activity android:name=".menu.OpenMenuUserActivity" />
        <activity android:name=".menu.user.news_event.DetailEvent"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".menu.user.group_chat.GroupChatActivity" />
        <activity android:name=".menu.user.find_people.DetailPeopleActivity"></activity>
    </application>

</manifest>